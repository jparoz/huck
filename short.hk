type Wrap a = Wrap a;

(* unwrap : Wrap (Int -> Int) -> (Int, Int); *)
unwrap w = case w of {
    (Wrap i) -> let id = i in (id 123, id 456);
};

(* unwrap2 : Wrap (Int -> Int) -> (Int, Int); *)
unwrap2 = \(Wrap i) -> let id = i in (id 123, id 456);
